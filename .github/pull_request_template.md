## What does this change?

<!-- One or two sentences. Link related issues with "Closes #123" if applicable. -->

## Why?

<!-- What problem does this solve, or what behavior does it add/change? -->

## How was this tested?

<!-- e.g. `npm test`, `npm run typecheck`, manual test with F5, real hook payloads from Copilot/Claude Code -->

## Checklist

- [ ] `npm run typecheck` passes (root + `webview-ui`)
- [ ] `npm test` passes (root + `webview-ui`)
- [ ] `CHANGELOG.md` updated, if this is user-visible
- [ ] No secrets, tokens or personal data included in code, tests or commit messages
